<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container px-5">
        <a class="navbar-brand" href="/">Snow Tricks</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item"><a class="nav-link" href="{{ path('home') }}">Accueil</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('all-tricks') }}">Figures</a></li>
                {% if app.user %}
                    {# User is logged in, show Logout link #}
                    <li class="nav-item"><a class="nav-link" href="{{ path('add-trick') }}">+</a></li>
                    <li class="nav-item"><a class="nav-link">Bonjour, {{ app.user.username }}</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Déconnectez</a></li>
                {% else %}
                    {# User is not logged in, show Login link #}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Se connectez</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('registration') }}">Créez un compte</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>